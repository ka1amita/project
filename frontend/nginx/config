events {
  worker_connections  1024;  ## Default: 1024
}

http {
    server {
        listen 80;
        listen [::]:80;

        root /opt/homebrew/var/www;

        index index.html;

        server_name _;

        location / {
                try_files $uri $uri/ /index.html;
        }
    }
}
